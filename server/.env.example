# ════════════════════════════════════════════════
#   ModaMedidas — Variables de entorno (servidor)
#   Copia este archivo como .env y completa los valores
# ════════════════════════════════════════════════

# Entorno: development | production | test
NODE_ENV=development

# Puerto del servidor
PORT=4000

# ── Base de datos ────────────────────────────────
# MongoDB local:
MONGODB_URI=mongodb://localhost:27017/moda_medidas
# MongoDB Atlas (producción):
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/moda_medidas?retryWrites=true&w=majority

# ── Autenticación JWT ────────────────────────────
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_UN_SECRETO_MUY_LARGO_Y_ALEATORIO
JWT_EXPIRES_IN=7d

# ── URLs ─────────────────────────────────────────
# URL del cliente (para CORS)
CLIENT_URL=http://localhost:3000

# ── Almacenamiento (opcional - para fotos) ───────
# Carpeta local de uploads (relativa al servidor)
UPLOADS_DIR=uploads

# ── SaaS - Stripe (para escalabilidad futura) ────
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# ── Email (para escalabilidad futura) ────────────
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_USER=apikey
# SMTP_PASS=re_...
